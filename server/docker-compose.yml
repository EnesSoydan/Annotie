services:
  relay:
    build: .
    container_name: annotie-relay
    ports:
      - "8765:8765"
    restart: unless-stopped
    # Kimlik doğrulamayı ETKİNLEŞTİRMEK için (public deploy önerilir):
    # aşağıdaki iki satırın yorumunu kaldırıp değerleri girin. Verilmezse
    # relay açık moddadır (LAN/test). Sadece anon/publishable key kullanın.
    # environment:
    #   SUPABASE_URL: "https://ckjnkvrsdyybzyvdmszm.supabase.co"
    #   SUPABASE_ANON_KEY: "sb_publishable_..."
